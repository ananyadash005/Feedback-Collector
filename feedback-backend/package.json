{
  "name": "feedback-backend",
  "version": "1.0.0",
  "description": "Backend API for Feedback Collector Application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd ../feedback-collector-frontend && npm install && npm run build && cd ../feedback-backend && npm run copy-frontend",
    "copy-frontend": "node scripts/copy-frontend.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "feedback",
    "api",
    "express",
    "mongodb"
  ],
  "author": "Ananya",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
